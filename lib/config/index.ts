require("dotenv").config();

export const env = process.env.NODE_ENV;

export const secret = process.env.SECRET;

export const api_version = process.env.API_VER;

export const DB_URL = process.env.DB_URL;
